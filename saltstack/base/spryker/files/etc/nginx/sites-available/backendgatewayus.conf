###############################################################################
##       This file is managed by saltstack - DO NOT MANUALLY CHANGE IT       ##
###############################################################################

server {
    # Listener for production/staging - requires external LoadBalancer directing traffic to this port
    listen {{ settings.environments[environment].backendgatewayus.port }};

    # Listener for testing/development - one host only, doesn't require external LoadBalancer
    listen 80;

    server_name {{ settings.environments[environment].backendgatewayus.hostname }};

    keepalive_timeout 0;
    access_log  /data/logs/{{ environment }}/backendgatewayus-access.log extended;

    root /data/shop/{{ environment }}/current/public/backendgatewayus;

{%- if settings.environments[environment].backendgatewayus.htpasswd_file is defined %}
    auth_basic            "Restricted Files";
    auth_basic_user_file  {{ settings.environments[environment].backendgatewayus.htpasswd_file }};
{%- endif %}

    set $application_env {{ environment }};
    include "spryker/backendgatewayus.conf";
}
